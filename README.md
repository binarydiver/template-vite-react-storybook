# Template for Vite + React + Storybook

## Setup Process

`pnpm create vite template-vite-react-storybook --template react-ts`\
`pnpm i`\
`rm -rf node_modules/.cache/storybook`\
`pnpx sb init --builder vite`\
`pnpm run storybook`
